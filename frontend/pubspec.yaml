name: prbd_2526_c05
description: PRBD - ReserVite App (groupe c05)

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.6.0
  hooks_riverpod: ^3.2.0
  flutter_hooks: ^0.21.0
  jwt_decoder: ^2.0.1
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  hive_ce: ^2.6.0
  async: ^2.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
